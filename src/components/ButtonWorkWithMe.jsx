import { Button } from '@nextui-org/react'

const ButtonWorkWithMe = ({ className = '' }) => {
  return (
    <Button className={className} variant='shadow' color='danger'>Contrátame</Button>
  )
}
export default ButtonWorkWithMe
